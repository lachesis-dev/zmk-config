# This file generates the GitHub Actions matrix
---
include:
  # totem left (central)
  - board: xiao_ble
    shield: totem_left
    artifact-name: totem_central_left
    cmake-args: -DCONFIG_ZMK_SPLIT_ROLE_CENTRAL=y

  # totem right (peripheral)
  - board: xiao_ble
    shield: totem_right
    artifact-name: totem_peripheral_right
 
  # reset
  - board: xiao_ble
    shield: settings_reset
